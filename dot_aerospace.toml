config-version = 2

start-at-login = true

enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

accordion-padding = 24
default-root-container-layout = "tiles"
default-root-container-orientation = "auto"

[mode.main.binding]
alt-h = "focus left"
alt-j = "focus down"
alt-k = "focus up"
alt-l = "focus right"

alt-shift-h = "move left"
alt-shift-j = "move down"
alt-shift-k = "move up"
alt-shift-l = "move right"

# Traverse windows in tree order. On an accordion workspace these bindings
# behave like komorebi's cycle-stack previous/next.
alt-leftSquareBracket = "focus --wrap-around dfs-prev"
alt-rightSquareBracket = "focus --wrap-around dfs-next"

# Explicit container layouts: horizontal/vertical tiles and accordion stack.
alt-x = "layout h_tiles"
alt-y = "layout v_tiles"
alt-a = "layout tiles accordion"

# Build a BSP-like tree by grouping the focused window with its neighbour.
# Unlike `split`, `join-with` works with container flattening enabled.
alt-left = "join-with left"
alt-down = "join-with down"
alt-up = "join-with up"
alt-right = "join-with right"

alt-q = "close"
alt-f = "fullscreen"
alt-t = "layout floating tiling"
alt-shift-r = "reload-config"

alt-1 = "workspace 1"
alt-2 = "workspace 2"
alt-3 = "workspace 3"
alt-4 = "workspace 4"
alt-5 = "workspace 5"
alt-6 = "workspace 6"
alt-7 = "workspace 7"
alt-8 = "workspace 8"
alt-9 = "workspace 9"

alt-shift-1 = "move-node-to-workspace 1"
alt-shift-2 = "move-node-to-workspace 2"
alt-shift-3 = "move-node-to-workspace 3"
alt-shift-4 = "move-node-to-workspace 4"
alt-shift-5 = "move-node-to-workspace 5"
alt-shift-6 = "move-node-to-workspace 6"
alt-shift-7 = "move-node-to-workspace 7"
alt-shift-8 = "move-node-to-workspace 8"
alt-shift-9 = "move-node-to-workspace 9"
